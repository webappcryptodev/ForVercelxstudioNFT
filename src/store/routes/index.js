import { USERS } from './users';
import { NFTS } from './nfts';
import { COLLECTIONS } from './collections';
import { BASE_URL, BASE_URL_LOCAL } from './routes';

export const ROUTES = {
  BASE_URL,
  BASE_URL_LOCAL,
  USERS,
  NFTS,
  COLLECTIONS,
};
